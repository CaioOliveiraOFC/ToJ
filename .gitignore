# Ambiente virtual
.venv/
venv/
reports/
 
# Variáveis de ambiente (NUNCA commitar chaves de API)
.env
.env.*
.env*
.venv/

# Cache do Python
__pycache__*
*.py[cod]
*.pyo
*.pyd
.ruff_cache/

# Dados gerados
savegame.json
saves/
# Exceto a evidência citada no BALANCE_REPORT.md: um relatório que ninguém
# consegue abrir não é evidência de nada. A lista é nominal de propósito —
# curinga aqui faria toda execução do simulador aparecer no `git status`.

# IDEs
.idea/
.vscode/
*.swp

# Graphify (cache de grafo)
graphify-out/

# Graphify (cache de grafo)
graphify-out/